# Large Test File for Redmargin

This file is used for testing various markdown features including checkboxes, headings, code blocks, and more.

## Task Lists

### Project Setup
- [ ] Initialize repository
- [ ] Set up CI/CD pipeline
- [ ] Configure linting rules
- [x] Create project structure
- [ ] Write initial documentation

### Development Tasks
- [ ] Implement core features
- [ ] Add unit tests
- [ ] Add integration tests
- [x] Set up logging
- [ ] Configure monitoring

### Review Checklist
- [ ] Code review completed
- [ ] Tests passing
- [ ] Documentation updated
- [ ] Performance benchmarks run
- [ ] Security audit done

## Code Examples

### Swift Example

```swift
import Foundation

struct Task: Identifiable, Codable {
    let id: UUID
    var title: String
    var isCompleted: Bool
    var dueDate: Date?

    init(title: String) {
        self.id = UUID()
        self.title = title
        self.isCompleted = false
    }

    mutating func toggle() {
        isCompleted.toggle()
    }
}

class TaskManager: ObservableObject {
    @Published var tasks: [Task] = []

    func addTask(_ title: String) {
        tasks.append(Task(title: title))
    }

    func removeTask(at index: Int) {
        tasks.remove(at: index)
    }

    func toggleTask(at index: Int) {
        tasks[index].toggle()
    }
}
```

### JavaScript Example

```javascript
class MarkdownRenderer {
    constructor(options = {}) {
        this.options = {
            sanitize: true,
            highlight: true,
            ...options
        };
        this.plugins = [];
    }

    use(plugin) {
        this.plugins.push(plugin);
        return this;
    }

    render(markdown) {
        let html = this.parse(markdown);

        for (const plugin of this.plugins) {
            html = plugin.transform(html);
        }

        if (this.options.sanitize) {
            html = this.sanitize(html);
        }

        return html;
    }

    parse(markdown) {
        // Parse markdown to HTML
        return markdown;
    }

    sanitize(html) {
        // Sanitize HTML
        return html;
    }
}
```

### Python Example

```python
from dataclasses import dataclass
from typing import List, Optional
from datetime import datetime

@dataclass
class Task:
    id: str
    title: str
    completed: bool = False
    due_date: Optional[datetime] = None
    tags: List[str] = None

    def __post_init__(self):
        if self.tags is None:
            self.tags = []

    def toggle(self):
        self.completed = not self.completed

    def add_tag(self, tag: str):
        if tag not in self.tags:
            self.tags.append(tag)

class TaskRepository:
    def __init__(self):
        self._tasks = {}

    def add(self, task: Task):
        self._tasks[task.id] = task

    def get(self, task_id: str) -> Optional[Task]:
        return self._tasks.get(task_id)

    def all(self) -> List[Task]:
        return list(self._tasks.values())

    def pending(self) -> List[Task]:
        return [t for t in self._tasks.values() if not t.completed]
```

## Tables

| Feature | Status | Priority | Notes |
|---------|--------|----------|-------|
| Checkbox toggle | In Progress | High | Race condition fix needed |
| Scroll position | Done | Medium | Works for local files |
| Remote files | In Progress | High | SSH support added |
| Git gutter | Done | Medium | Shows diff indicators |
| Find/Replace | Partial | Low | Find works, replace pending |

## Blockquotes

> "The best code is no code at all. Every new line of code you willingly bring into the world is code that has to be debugged, code that has to be read and understood, code that has to be supported."
>
> — Jeff Atwood

> **Note:** This is an important note that should stand out from the rest of the content.

## Lists

### Ordered List
1. First item
2. Second item
3. Third item
   1. Sub-item A
   2. Sub-item B
4. Fourth item

### Unordered List
- Apple
- Banana
- Cherry
  - Dark cherry
  - Light cherry
- Date

### Mixed Nested List
1. Project phases
   - Planning
     - [ ] Requirements gathering
     - [ ] Architecture design
   - Development
     - [ ] Core implementation
     - [ ] Testing
   - Deployment
     - [ ] Staging
     - [ ] Production

## Links and Anchors

### Internal Links
- [Jump to Code Examples](#code-examples)
- [Jump to Tables](#tables)
- [Jump to Task Lists](#task-lists)

### External Links
- [Swift Documentation](https://docs.swift.org)
- [GitHub](https://github.com)
- [Stack Overflow](https://stackoverflow.com)

## Images

Images would go here if this were a real document:
- Screenshot of the app
- Architecture diagram
- Workflow diagram

## Horizontal Rules

---

Content between rules.

***

More content.

___

## Emphasis

This is **bold text** and this is *italic text*.

This is ***bold and italic*** text.

This is ~~strikethrough~~ text.

This is `inline code` within a sentence.

## Longer Prose Section

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.

Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.

Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam, eaque ipsa quae ab illo inventore veritatis et quasi architecto beatae vitae dicta sunt explicabo.

Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur magni dolores eos qui ratione voluptatem sequi nesciunt.

## Final Checklist

- [ ] All features tested
- [ ] No regressions found
- [ ] Performance acceptable
- [ ] Memory usage normal
- [ ] Ready for release

---

*End of test file*
